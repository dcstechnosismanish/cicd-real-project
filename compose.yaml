services:
  app:
    build: .
    container_name: cicd-app
    ports:
      - "127.0.0.1:3000:3000"

    restart: unless-stopped

    healthcheck:
      test: [ "CMD", "wget", "--spider", "-q", "http://127.0.0.1:3000" ]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 5s
